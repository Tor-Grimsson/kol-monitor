# lobby — kol-monitor

Intake queue for **kol-monitor** (`monitor.kolkrabbi.io`): the monitoring
surface — UI issues, app-shell and routing behaviour, and this repo's
consumption of the `@kolkrabbi/*` packages.
Not documentation — a work queue, deliberately outside `docs/`.

**This file is the ledger. The ledger is the truth, never a raw `ls`.**

| | |
|---|---|
| file one | `clip-drop.sh --kol-monitor NAME` |
| read it | `/lobby-list` · `bin/lobby` · `prefix Ctrl+K` |
| the spec | `~/.dotfiles/docs/operations/systems/lobby/` |

## States

| | state | means | lives in |
|---|---|---|---|
| 🔵 | `filed` | captured, unread | `inbox/` |
| 🟡 | `read` | understood — the row below restates it | `inbox/` |
| 🟠 | `addressed` | a change shipped that is *meant* to close it | `inbox/` |
| 🟢 | `closed` | met the bar; resolution appended | `done/` |
| ⚪ | `parked` | deliberately not-**now**, reason recorded — revisitable | `archive/` |
| ⚫ | `retired` | closed without a fix, not-**ever** — terminal, and never ages | `archive/` |
| 🔴 | `needs-ruling` | **flag, not a state** — blocked on the user's call | wherever it is |
| 📌 | `remainder` | **flag, not a state** — closed at its destination, still owed **here** | `outbox/` |

**`read` is never `closed`.** Understanding a ticket ships nothing.
**Bar for 🟢 closed in this repo — purpose served:** the change shipped and was
verified by running it, cited by file. **The agent closes on that evidence.**
Parking, declaring stale, reopening and any design decision stay the **user's call**.

## Queue — 1 entry

| | Entry | About | Staged | State |
|---|---|---|---|---|
| 🟠 | [RackTierFullConsumption](inbox/RackTierFullConsumption.md) | The six full-consumption greps, run **and worked** 2026-08-30 against theme 0.96.0 · component 0.131.0. Check 2 **56 → 9** (every survivor a stated deviation), check 6 **2 → 0**, shadowed selectors **31 → 0**, local CSS **1160 → 841** lines, 7 dead atoms retired | 2026-08-30 | `addressed` — build green, lint count unchanged at 2335. **Not closed:** four surfaces still want your eye (stage clock detail · Workbench pill · Oscilloscope input+trigger · CaseHpDialog slider). **Both calls are RULED** — the two rack widgets keep their names as a permanent stated deviation (rename declined 2026-08-30), and `LibraryPage`'s button overrides were dropped; written back into the ticket 2026-09-01, do not re-raise |

## Closed

| | Entry | About | Staged | Closed | State |
|---|---|---|---|---|---|
| 🟢 | [SettingsMastheadCluster](done/SettingsMastheadCluster.md) | The settings masthead's control row is `SettingsScaffold`'s now (kol-shell 0.27.0) — picker · theme toggle · gear, order and gap and tone the DS's. Monitor and kol-mirror had passed **nothing** into the old raw `header.actions` slot, which is the whole of why three settings pages did not match | 2026-09-01 | 2026-09-01 | `addressed` — adopted on the full set (**shell 0.31.0 · theme 0.116.0 · component 0.149.0 · framework 0.36.0**, each tarball-diffed). `themeToggle` only: no `picker` (nothing to pick), no `onOpenSettings` (no drawer — a gear that opens nothing is worse than none). The toggle **moved** out of the body's Display row rather than being added beside it, matching kol-fxr's reference render. Build green, lint unchanged at 2335. **Closed 2026-09-01 — browser-verified** at 1440 and 390 (`_tmp/2026-09-01-mobile-qa/qa-settings-1440.png` · `qa-settings-390.png`): the cluster renders, the toggle sits alone in the masthead and appears exactly once on the page |
| 🟢 | [ShellSettingsPairRetired](done/ShellSettingsPairRetired.md) | kol-shell 0.21.0 retired its own `SettingsSection` + `LabelRow` — second implementations of kol-component's `LabeledControlSection` + `SettingsRow`, under the user's *"dont ship duplicate components"* ruling. No alias, no deprecation window | 2026-08-30 | 2026-09-01 | `closed` — **already done when it landed**. The swap shipped 2026-08-30 (session 69), forced by the bump: 0.24.0 broke the build on `SettingsPage.jsx` and the migration was the only way past it. `src/pages/SettingsPage.jsx:1-2` is the ticket's diff verbatim; both prop renames and both stated render deltas are in the body. Green on shell 0.31.0. Nothing was owed |
| 🟢 | [ShellHomeSystemAdoption](done/ShellHomeSystemAdoption.md) | The shared app tier (kol-shell 0.8.0 · component 0.105.0 · theme 0.71.0 · framework 0.28.0 · icons 0.20.0 · brand 0.1.3) adopted on kol-fxr's wiring: Home on `CatalogPage`, Settings on `SettingsShortcuts` / `SettingsLinks` / `SettingsColophon`, `AppShell railToggleKey` + `touch="overlay"`, `nav-*` + logomark from the DS, the theme boot script, `voice="mono"`, no `GridCard` left, the t-shirt type stops retired | 2026-08-27 | 2026-08-27 | `closed` — build green, `dist/index.html` carries the boot script; Library + Create stay on `ContentFilters` + the cards directly (seams filed as `ShellHomeSystemMonitorGaps`). Visual check the user's |

## Archived

_(none yet — ownership, deferral and context notes land in `archive/`)_

## Filed elsewhere

Tickets this ledger does **not** govern — each row names the destination ledger
that does. The **Remainder** is this repo's to do; the state is theirs to report.
All four went to **kol-ds-ui** — `~/dev/projects/kol-ds-ui/lobby/INDEX.md`.

| | Receipt | Last known | Remainder here |
|---|---|---|---|
| 🟢 | [ShellDrawerCloseOnSamePath](outbox/ShellDrawerCloseOnSamePath.md) | 🟢 `closed` 2026-09-02 — **kol-shell 0.40.0**: the drawer folds on a same-path rung | none — adopted 2026-09-02, shim removed, WebKit-checked |
| 🟢 | [ContentCardActionsInsetShorthand](outbox/ContentCardActionsInsetShorthand.md) | 🟢 `closed` 2026-09-02 — **kol-component 0.162.0**: the catalog card's `actions` inset takes single values | none — bumped 2026-09-02; monitor's INSERT is its own strip under the card by the user's ruling, the slot is unused here |
| 🟢 | [RailGrabTapIsALine](outbox/RailGrabTapIsALine.md) | 🟢 `closed` 2026-09-02 — **kol-shell 0.39.0 · kol-theme 0.128.0**: the opener is the strip, thicker under a coarse pointer; the disc and its glyph are gone from NavRail | none — adopted 2026-09-02; the fork matches NavRail's button exactly |
| 🟢 | [StepperInlineVariant](outbox/StepperInlineVariant.md) | 🟢 `closed` 2026-09-02 — **kol-component 0.161.0**: `Stepper layout="inline"` — `‹ value ›` on one line, on the ladder; the stacked layout stays the default | none — adopted + measured 2026-09-02 (chevrons flanking the 8px value); Recorder ×3 + Raster on it, local Selector retired |
| 🟢 | [DropdownXsList](outbox/DropdownXsList.md) | 🟢 `closed` 2026-09-01 — **kol-component 0.160.0 · kol-theme 0.127.0**: xs list rows on the rung, panel min-width = widest option | none — adopted + measured 2026-09-01: four xs rows, 8px / 20px, none truncated |
| 🟢 | [ControlsXsRung](outbox/ControlsXsRung.md) | 🟢 `closed` 2026-09-01 — **kol-theme 0.126.0 · kol-component 0.159.0 · kol-controls 0.3.0**: the `xs` rung, `Input onCommit`, `Stepper options`; TextInput · PanelDropdown · Selector retired from kol-controls | none — adopted + verified 2026-09-01: seven sites on `Input xs` / `Dropdown xs` / `Stepper xs options`, two seams retired to `_tmp/`, A/B re-shot |
| 🟢 | [ControlsJackSeams](outbox/ControlsJackSeams.md) | 🟢 `closed` 2026-09-01 — **kol-controls 0.2.0**: `ringRef` on `JackSocket`, `jackComponent` on `LabeledJack` | none — adopted + verified 2026-09-01: the wiring seam passes `ringRef`, the `querySelector` is gone, `LabeledJack` is a 3-line seam over the package's; the local copy in `_tmp/2026-09-01-controls-adoption/` |
| 🟢 | [KolControlsPackage](outbox/KolControlsPackage.md) | 🟢 `closed` 2026-09-01 — **kol-controls 0.1.0 · kol-theme 0.124.0**: 15 exports carried class-for-class, three renamed (Slider→Fader, Dropdown→PanelDropdown, LabeledControl→PanelLabel), tokens `--monitor-*` → `--kol-ctl-*`, LEDs off the palette as ruled, routing/power as props | none — adopted + **verified by running** 2026-09-01: seams in `parametric/`, wired `JackSocket`, locals in `_tmp/2026-09-01-controls-adoption/`. Two seams asked back (`ControlsJackSeams`); the TextInput/Dropdown comparisons are their own pass |
| 🟢 | [AppShellSet](outbox/AppShellSet.md) | 🟢 `closed` 2026-08-14 — kol-shell 0.1.0 + theme 0.41.0 + framework 0.20.0 | none — adopted 2026-08-15: locals retired to `_tmp/2026-08-15-shell-adoption/`, `aria-current` native via the DS rail, shortcuts single-sourced at `src/data/shortcuts.js`, all pnpm patches retired. kol-mirror still owes its own share |
| 🟢 | [ComponentSideEffectsField](outbox/ComponentSideEffectsField.md) | 🟢 `closed` 2026-08-15 — component 0.39.0 | none — adopted 2026-08-15 |
| 🟢 | [ComponentTailwindSourceTrap](outbox/ComponentTailwindSourceTrap.md) | 🟢 `closed` 2026-08-12 — theme 0.38.0 + framework 0.19.0 | none — adopted 2026-08-14. Visual roll check (travels + coin-rotates) pending user QA |
| 🟢 | [KolComponentDeepExports](outbox/KolComponentDeepExports.md) | 🟢 `closed` 2026-08-12 — component 0.35.0, exports map registry-verified | none — adopted 2026-08-12 |
| 🟢 | [ShellHeaderFilterRefinements](outbox/ShellHeaderFilterRefinements.md) | 🟢 `closed` 2026-08-15 — kol-shell 0.1.3 after three QA rounds. **Superseded by `ContentFiltersForkRetirement`** — every round tuned a forked component | none — the fork is retired; see the row below |
| 🟢 | [ContentFiltersForkRetirement](outbox/ContentFiltersForkRetirement.md) | 🟢 `closed` 2026-08-15 — **kol-component 0.44.2 + kol-shell 0.3.0**: kol-shell's forked ContentFilters + TabStrip retired, the kol-component organism is canon, filter values are real `Tag` atoms again | none — adopted 2026-08-15: all four call sites on the organism, seam retired to `_tmp/`, uppercase workarounds reverted, build green. Visual check the user's |
| 🟢 | [ShellHomeSystemMonitorGaps](outbox/ShellHomeSystemMonitorGaps.md) | 🟢 `closed` 2026-08-27 — **kol-shell 0.9.0**: overlay reads `combo`, `CatalogPage` `filtersProps` + `expanded` through `toCard` | none — bumped, map dropped 2026-08-27; Library on `CatalogPage` since the `CatalogPageMonitorParity` bump the same day |
| 🟢 | [SettingsShortcutsComboWrap](outbox/SettingsShortcutsComboWrap.md) | 🟢 `closed` 2026-08-27 — **kol-shell 0.9.1**: the combo cell is `whitespace-nowrap`, the label yields | none — adopted 2026-08-27: the nowrap `comboLabel` dropped from `SettingsPage`. Visual check the user's |
| 🟢 | [CatalogPageCardFit](outbox/CatalogPageCardFit.md) | 🟢 `closed` 2026-08-27 — **kol-shell 0.9.2**: `fit` through `toCard` — but its `natural` is contain, not GridCard's | none adopted; superseded by `CatalogPageMonitorParity` |
| 🟢 | [CatalogPageMonitorParity](outbox/CatalogPageMonitorParity.md) | 🟢 `closed` 2026-08-27 — **kol-component 0.108.0 · kol-shell 0.10.0**: `fit natural / compact` are GridCard's rules verbatim, `CatalogPage` hides the 2×2's neighbours | none — adopted 2026-08-27: Library on `CatalogPage`, `computeHiddenSet` + the `.preview-*` / `.home-catalog` fit rules deleted, `fit` through `toCard`. Hover ruling kept on `.kol-card`. Build green. Visual check the user's |
| 🟢 | [ContentMediaFocusBinding](outbox/ContentMediaFocusBinding.md) | 🟢 `closed` 2026-08-27 — **kol-component 0.111.0 · kol-theme 0.73.0**: `--kol-media-focus` read by both the fits and the zoom, unset = today | none — adopted 2026-08-27: bound `top left` on `:root` in `monitor-overrides.css`. Build green |
| 🟢 | [ContentFiltersFirstGroupFixedWidth](outbox/ContentFiltersFirstGroupFixedWidth.md) | 🟢 `closed` 2026-08-27 — **kol-component 0.111.0 · kol-theme 0.73.0**: `.kol-filters-first` = `(100cqw − 120px) / 6` measured on `.kol-filters-row` | none — adopted 2026-08-27 on the 0.113.0 / 0.74.0 bump: the `w-40` interim dropped from all four first groups. Build green |
| 🟢 | [ShellPageWash](outbox/ShellPageWash.md) | 🟢 `closed` 2026-08-27 — **kol-shell 0.11.0**: `AppShell` paints the primary back + `pageWash` → `--kol-shell-page-wash`, `PageShell` reads it with a primary fallback | none — adopted 2026-08-27: `pageWash="var(--kol-fg-12)"`, RailFrame back to `contents`, override rule deleted, rack root reads the var. Build green. Visual check the user's |
| 🟢 | [FullConsumptionContract](outbox/FullConsumptionContract.md) | 🟢 `closed` 2026-08-27 in **two parts** — Part 1 `@kolkrabbi/kol-theme/core` (theme 0.77.0), the app tier without the five domain packs; Part 2 a **checklist, not a certificate** — the six greps at kol-ds-ui `docs/documentation/00-overview/04-full-consumption.md` | Part 1 adopted 2026-08-27 (built CSS 283.7 → 230.4 kB). **Greps run 2026-08-30 — 4 of 6 clean**; check 6 closed in the same sitting. Checks 2 + 4 carried into `inbox/RackTierFullConsumption.md`, which is the live row — **remainder here: none** (📌 cleared 2026-09-01: it was pointing at its own successor) |
| 🔵 | [ListGridCards](outbox/ListGridCards.md) | 🔵 `filed` 2026-08-15 — **open collection**, never closes; accrues card pairs from active repos | none — monitor's pair (kol-shell GridCard grid + list row) is in the collection; append a fresh pair if the cards change shape |
| 🟢 | [PageShellScrollbarGutter](outbox/PageShellScrollbarGutter.md) | 🟢 `closed` 2026-08-28 — **kol-shell 0.18.0**: `scrollbar-gutter: stable` on `PageShell`'s root. Acts in `fixed` mode only (there the element IS the scroll container) — inert in `scroll`, so no second gutter | none — adopted 2026-08-28 on the 0.18.0 / component 0.127.0 pair, registry-verified in the published source and in monitor's bundle. Build green. **Unmeasured at both ends — the three strips still need the user's eye** |
| 🟢 | [AppShellNavKeysHomeFirst](outbox/AppShellNavKeysHomeFirst.md) | 🟢 `closed` 2026-08-28 — **kol-shell 0.19.0**: with a `logomark`, `navKeys` walks `['/', ...items]` — the order on screen; without one, `items[n-1]` as before. Inferred from `logomark`, not an explicit array, so it cannot drift from what the rail renders | none — adopted 2026-08-28: bumped, local listener + `NAV_KEY_PATHS` deleted, `navKeys` restored, sheet row reads 'Home first'. Registry-verified in the published source and the bundle. Build green. **⌥1–4 still needs the user's eye** |
| 🟢 | [CatalogPageMobileColumns](outbox/CatalogPageMobileColumns.md) | 🟢 `closed` 2026-09-01 — **kol-shell 0.33.0**: cols is a ceiling with floors (160 grid / 240 list); desktop sixth-share clears them so nothing moves at width | none — adopted + **browser-verified** 2026-09-01: Home one full-width preset card, Library one module card per row at 390 (`qa2-*.png` vs the broken `qa-*.png`). Build green |
| 🟢 | [SettingsShortcutsComboOverflow](outbox/SettingsShortcutsComboOverflow.md) | 🟢 `closed` 2026-09-01 — **kol-shell 0.33.0 + kol-component 0.151.0**: the real geometry was SettingsRow's fixed 160 label in a 176 column; `labelWidth="auto"` — label yields, combo hugs | none — adopted + **measured** 2026-09-01: combo right edge = its column edge (272), neighbour starts 320, overlap false. Build green |
| 🟢 | [ComponentUseGrabEdgeSubpath](outbox/ComponentUseGrabEdgeSubpath.md) | 🟢 `closed` 2026-09-01 — **kol-component 0.150.1**: `useGrabEdge` under `src/hooks/`, deep path `hooks/useGrabEdge`. 0.150.0 shipped the move with a broken `./motion.js` relative import (every barrel consumer failed at build) — caught here, fixed and deprecated same-sitting | none — adopted 2026-09-01: bumped to 0.150.1 (fix registry-verified in the shipped source), barrel import swapped for the subpath in `src/rack/RackRail.jsx`. Build green, lint 2335 |
| 🟢 | [IconButtonNoFlexShrink](outbox/IconButtonNoFlexShrink.md) | 🟢 `closed` 2026-09-02 — **kol-theme 0.129.0**: `.kol-btn-icon { flex: none }` — the square neither grows nor shrinks | none — bumped + re-measured 2026-09-02 in WebKit at 390 and 1440: gear and video buttons 32×32 beside the 32px Modules trigger |

## History

| Date | Event |
|---|---|
| 2026-08-12 | `KolComponentDeepExports` + `ComponentTailwindSourceTrap` filed into kol-ds-ui; both closed and adopted the same week |
| 2026-08-14 | `AppShellSet` filed into kol-ds-ui — the shared app shell graduated to the DS as a bundled set |
| 2026-08-15 | `ComponentSideEffectsField` filed into kol-ds-ui — the barrel was unshakeable without the manifest flag |
| 2026-08-15 | **`ShellHeaderFilterRefinements` filed into kol-ds-ui** — kol-shell 0.1.0 adoption QA: PageHeader h1/subtitle gap lost on recreation, filter tag chips md→sm, LIST/GRID toggle to the divider row with groups pinned below-left. Remainder here on ship: bump + verify |
| 2026-08-15 | **`ListGridCards` filed into kol-ds-ui — an open collection, not a task.** List + grid card pairs from active repos, gathered DS-side toward an eventual card-family consolidation; monitor's preset cards (kol-shell `GridCard` grid + list row, dark) are the first pair. Same session: the kol-shell adoption's missing gaps/wrapping root-caused to the `@source` manifest being a silent no-op under pnpm (`KolSourcesPnpmResolution`, already fixed upstream) — theme bumped 0.42.1→0.42.2, shell utilities back in the build |
| 2026-08-15 | **Ledger created and the lobby registered** in `~/.dotfiles/files/folders.md` § `lobby`. The folder already existed with four receipts in `outbox/` but **no ledger and no `inbox/`**, so `bin/lobby` could not read it — the four filings above were reconstructed from those stubs. Flag `--kol-monitor` falls out of the path |
| 2026-08-27 | **`ShellHomeSystemAdoption` filed** from kol-fxr — the shared Home · Library · Settings tier is in the DS; adopt it the way fxr did the same day. |
| 2026-08-27 | **`ShellHomeSystemAdoption` closed** — bumped to the set, Home on `CatalogPage`, Settings on the three DS pieces, AppShell key + touch policy, DS glyphs + logomark, boot script, mono masthead, `GridCard` gone, legacy type stops retired to `_tmp/2026-08-27-shell-home-adoption/`. Build green |
| 2026-08-27 | **`ShellHomeSystemMonitorGaps` filed into kol-ds-ui** — the two seams that kept Library off `CatalogPage` (`mutuallyExclusiveFilters`, `expanded` in `toCard`) and the `combo`/`keys` field mismatch between `SettingsShortcuts` and `ShortcutsOverlay`. Remainder here on ship: Library → `CatalogPage` |
| 2026-08-27 | **`SettingsShortcutsComboWrap` filed into kol-ds-ui** — three-character combos wrap in the shortcuts grid (seen rendering); nowrap `comboLabel` carried here meanwhile |
| 2026-08-27 | **`CatalogPageCardFit` filed into kol-ds-ui** — Home's preset cards crop the rack (seen by the user); `object-fit: contain` override on `.home-catalog` meanwhile. Library + Create cards moved `cover` → `natural` (the retired GridCard's default) |
| 2026-08-27 | **`ShellHomeSystemMonitorGaps` + `CatalogPageCardFit` returned** (kol-shell 0.9.0 / 0.9.2) — bumped to 0.9.2, `combo → keys` map dropped. `fit: 'natural'` rejected on sight (contain ≠ GridCard's scale-0.3 top-left); the override rules stay. **`CatalogPageMonitorParity` filed** — the two real fits + neighbour hiding on `CatalogPage`; Library moves on that bump |
| 2026-08-27 | **`CatalogPageMonitorParity` + `SettingsShortcutsComboWrap` returned and adopted** (kol-component 0.108.0 · kol-shell 0.10.0; theme 0.72.0 + icons 0.22.0 rode along) — Library on `CatalogPage`, `computeHiddenSet` and the `.preview-*` / `.home-catalog` fit rules deleted (`fit` through `toCard`), the nowrap `comboLabel` dropped. The hover ruling stays, keyed on `.kol-card`. Build green |
| 2026-08-27 | **`ContentMediaFocusBinding` filed into kol-ds-ui** — the user rules the image focus a per-repo binding; the DS hardcodes the anchor in the fits and the zoom. No carry here (top-left is already monitor's value); bind the token on ship |
| 2026-08-27 | **`ContentFiltersFirstGroupFixedWidth` filed into kol-ds-ui** — user ruling on Home's TYPE column beside `/work`'s: the first filter group is a set width — one catalog column — not a hug. `className: 'w-40'` (160, the reachable interim) carried on every first group meanwhile; announced in the LLM_RULES bulletin |
| 2026-08-27 | **`ShellPageWash` filed into kol-ds-ui** — the main-background lift as a shell mechanism for monitor · fxr · mirror: primary back, per-app transparent wash on the page root. Carried here as `.monitor-page-back` (RailFrame) + the fg-12 rule in `monitor-overrides.css` |
| 2026-08-27 | **`ShellPageWash` returned and adopted** (kol-shell 0.11.0, same afternoon) — `pageWash` prop on `AppShell`, the local back + wash carry retired, rack root on the variable. fxr + mirror handed their adoption over SendMessage |
| 2026-08-27 | **`FullConsumptionContract` filed into kol-ds-ui** — after an audit here found the app tier fully on the DS and the rack tier hand-rolled. Asks the DS for a core theme entry (skip the 81 KB of packs a rack app can never render) and for a written definition of full consumption the DS can certify a repo against |
| 2026-08-27 | **`ContentFiltersFirstGroupFixedWidth` + `ContentMediaFocusBinding` returned and adopted** — bumped kol-component 0.108.0→0.113.0 · kol-theme 0.72.0→0.74.0 (0.112.0's `SettingsSection`→`LabeledControlSection` rename is BREAKING but does not touch monitor: its `SettingsSection` is kol-shell's own, not kol-component's). `w-40` interim dropped, `--kol-media-focus: top left` bound |
| 2026-08-28 | **`PageShellScrollbarGutter` filed into kol-ds-ui** — chasing a layout difference the user saw on Create's filter strip. Monitor's side is clean (no local `.kol-filters*` rule anywhere, both prior interims deleted); the split is `PageShell mode="fixed"` vs `mode="scroll"` and a first filter group sized in container units. Diagnosed in source only — the delta is unmeasured and is zero under overlay scrollbars. Nothing carried here |
| 2026-08-28 | **`PageShellScrollbarGutter` returned and adopted the same sitting** (kol-shell 0.18.0, paired with component 0.127.0 which it peers). The DS added an asymmetry worth keeping: the declaration only acts in `fixed` mode, where the element is itself the scroll container — in `scroll` mode it is inert and the viewport keeps its own gutter. Declined one level up: `html { scrollbar-gutter: stable }` would also equalise a short scrolling page against a long one, but that is an estate-wide ruling, not this ticket. Neither side ran a browser |
| 2026-08-28 | **`AppShellNavKeysHomeFirst` filed into kol-ds-ui** — the user found ⌥1 landing on Library. `navKeys` (our own ticket, 0.12.0, the same day) counts `items`, and the rail's first rung is the logomark, so Home was never in the sequence its own ask named. Fixed here meanwhile with a local listener; the DS prop is off because both listen on `window` |
| 2026-08-28 | **`AppShellNavKeysHomeFirst` returned and adopted the same sitting** (kol-shell 0.19.0). The DS chose the inferred form over the explicit path array offered as an alternative — inferring from `logomark` needs no consumer change and cannot drift from what the rail renders, which is the drift that caused the defect. Local carry deleted, `navKeys` back. Left open, unasked: `bottomItems` (Settings) has no key |
| 2026-08-30 | **The six full-consumption greps run — 4 of 6 clean**, against kol-theme **0.96.0** · kol-component **0.131.0** (both bumped the same sitting; additive only, tarball-diffed — theme adds `.kol-row-specs`, component adds `QuadrantSync` and renames the ContentCard/Row/Text variants to content kinds with `default`/`print`/`work`/`typeface` aliased, and monitor's `catalog` is untouched). Clean: 1 (no unrendered domain pack) · 3 (5 `:root` hits, all bindings) · 5 (0 `z-[…]`, 0 `bg-black/`; the 47 `rgba(0,0,0,…)` are inset panel shadows on eurorack hardware, not scrims) · **6, closed in the sitting** — the two `text-[10px]` swapped for `kol-helper-10`, and `ToggleCheckbox.jsx:32`'s `kol-mono-xs` found to be a retired t-shirt name resolving to **no rule anywhere** while `.toggle-checkbox-label` defines no type of its own, so that label had been rendering unstyled → `kol-mono-10`. Open: checks 2 + 4, filed to this repo's own queue as `RackTierFullConsumption`. `FullConsumptionContract`'s ledger row corrected from a stale 🔵 to 📌 |
| 2026-08-30 | **`RackTierFullConsumption` worked the same sitting it was filed.** Seven atoms in `src/components/atoms/` turned out to have **zero importers** — `Tag` (the fork check 4 named), `Checkbox`, `Input`, `ToggleCheckbox`, `ToggleSwitch`, `QuantityInput`, `QuantityStepper` — retired to `_tmp/2026-08-30-dead-atoms/`, which cost no rewiring and took five token reads with them. 18 more token reads became DS classes across `StageClock` · `ModuloSidebar` · `Workbench` · `VideoModulo` · `TextInput` · `OscilloscopeModule` · `ColorPicker` and the two dev pages. In `components.css`, **29 of the 31 shadowed selectors were dead rules** — defined by kol-theme, used by nothing here — cut to `_tmp/2026-08-30-shadowed-css/`; the 3 live ones (`.control-slider*`, `.slider-black`) were renamed `.monitor-slider*`, rules unchanged. Net: check 2 **56 → 9**, check 6 **2 → 0**, shadowed **31 → 0**, `components.css` **996 → 677**. The 9 surviving token reads are each a stated deviation — three are `:root` bindings check 3 blesses, two have no background-only class (`.bg-surface-*` is an ink+ground pair and those elements drive their own ink), four are inline overrides of a DS button's own chrome where a class would tie on specificity and lose to sheet order. Build green, lint unchanged at 2335 |
| 2026-09-01 | **`ShellSettingsPairRetired` closed on arrival and `SettingsMastheadCluster` adopted**, on a set that had run five releases ahead of what either ticket named — shell **0.26.0 → 0.31.0** · theme **0.100.0 → 0.116.0** · component **0.136.0 → 0.149.0** · framework **0.35.0 → 0.36.0**, each tarball-diffed before landing. Theme is additive; component's only removal is `MediaBrowser`, never imported here. The masthead took `themeToggle` alone — no picker, no gear, because a gear that opens nothing is worse than no gear — and the toggle **moved** out of the body's Display row rather than joining it, since two of one control on one page is the inconsistency the ticket exists to remove. **`ComponentUseGrabEdgeSubpath` filed into kol-ds-ui** en route: 0.147.0 promoted `useGrabEdge` out of `NavRail`, `RackRail.jsx`'s fork deleted its byte-identical copy, and the hook turned out to be unreachable by subpath. Build green, lint unchanged at 2335, nothing opened in a browser |
| 2026-09-01 | **`ComponentUseGrabEdgeSubpath` returned same-day — and the returning release is broken.** 0.150.0 moved `useGrabEdge` to `src/hooks/` as asked, but kept its relative `./motion.js` import while `motion.js` stayed in `utilities/`: `[UNRESOLVED_IMPORT]` through the barrel, so EVERY 0.150.0 consumer fails at build, deep path or no. Rolled back to 0.149.0 (green), adopted kol-theme 0.117.0 (additive), sent the defect + one-line fix to the live kol-ds-ui session over SendMessage with a deprecate ask (the kol-shell 0.16.0 precedent). Receipt re-opens as 📌 until the fixed release ships |
| 2026-09-01 | **`ComponentUseGrabEdgeSubpath` closed and adopted** — 0.150.1 ships the one-line fix within the sitting, 0.150.0 deprecated with a pointer. Monitor on 0.150.1, deep import in place, rollback comment gone. The full-consumption sweep's one remaining carried import is now a plain DS subpath |
| 2026-09-01 | **First real browser pass in this repo's history** (user override of the no-server law — preview on 4317, PID killed after; Playwright at 1440 and 390). Verified: settings masthead cluster ✓ · rack + cables + Monitor wireframe post-G1 ✓ · stage CRT/clock/dock ✓ · `touch="drawer"` folds, opens (240px, labelled rows, scrim, travelling trigger) and closes on navigate ✓ · theme 0.117.0's filter-row mobile rungs ✓. Fixed en route: **the stage's zoom-to-fit measured a half-built board** ([stage]-only deps; the preset lands rows on its own rAF) — zoom was 0.16 where 0.70 belongs; refit now keyed on rows landing. Found and filed: `CatalogPageMobileColumns` + `SettingsShortcutsComboOverflow`. Screenshots: `_tmp/2026-09-01-mobile-qa/` |
| 2026-09-01 | **Both morning tickets closed, adopted and browser-verified in one round trip** — shell 0.31.0 → **0.33.0** · component 0.150.1 → **0.151.0** (tarball-diffed: CatalogPage + Logomark + SettingsShortcuts / LabeledControl + SettingsPanel; `NavRail` byte-identical, fork drift zero). Library and Home readable at 390, the shortcuts sheet overlap measured gone at 1440. Preview on 4317, PID killed after |
| 2026-09-01 | **Ledger squared — three stale flags cleared, queue 2 → 1.** `FullConsumptionContract`'s 📌 was pointing at its own successor: Part 1 adopted 08-27, Part 2's greps run and worked 08-30, and what was left carried into `RackTierFullConsumption` — the receipt owed nothing and is 🟢, remainder `none`. `SettingsMastheadCluster` closed 🟠 → 🟢: its one blocker was "never opened in a browser", and the 09-01 pass rendered `/settings` at 1440 and 390 (`qa-settings-*.png`) — cluster present, toggle exactly once on the page. `RackTierFullConsumption`'s **two "user's call" items were both already ruled** and never written back — the rack-widget rename was declined 08-30 (now a permanent stated deviation) and the `LibraryPage` overrides were dropped (verified in source: three plain `variant="grey"` buttons). It stays 🟠 on the four surfaces that still want the user's eye, and nothing else |
| 2026-09-01 | **Inks run closed and `KolControlsPackage` filed.** `pnpm check:inks` 230 → 0 in one sitting (A shared widgets · B per-module · C semantic), build green, lint 2346 = pre-run, both themes rendered at 1440. Two rulings came back mid-run: the LED colours are the set's own, not the DS palette (35 reads in 13 files moved from `--kol-palette-*` to `--monitor-led-*`), and the "DS component sync" that was to follow became a package proposal — the rack's controls as `@kolkrabbi/kol-controls`, filed with the full inventory. Modules and composition stay here; mirror and fxr add to the package later |
| 2026-09-01 | **`KolControlsPackage` returned and adopted in one sitting** — the DS shipped `@kolkrabbi/kol-controls` 0.1.0 + kol-theme 0.124.0 within the hour. Monitor swapped every local control for the package (re-export seams; `IconButton` icon-bound; `ModuleHeader` powered; `JackSocket` a wiring seam), took the three renames at their sites, moved token reads to `--kol-ctl-*`, retired 14 locals to `_tmp/`. kol-component bumped to 0.156.0 for the peer (additive: ShellDrawer gains `side="bottom"`, ProfileCard). Verified live: knob drag, a patch drag creating a cable, the table, the sheet, both themes, 0 errors. `ControlsJackSeams` filed back for a `ringRef` + `jackComponent` seam |
| 2026-09-01 | **`ControlsJackSeams` returned and adopted within the hour** — kol-controls 0.2.0: the wired jack hands its ring over through `ringRef`, `LabeledJack` takes the wired jack through `jackComponent`; the last local control copy is gone. Verified with puppeteer-core (the Playwright MCP's Chrome hung on relaunch): 318 rings registered, a patch drag lands, 0 errors |
| 2026-09-01 | **Controls A/B shot and `ControlsXsRung` filed** — `/dev/controls-ab` renders the four pairs with the same data, light + dark; all four keep today for one reason (the DS floor is `sm`). User ruling: the DS ships `xs`, Selector becomes a `Stepper` variant on the ladder, IconButton stays in kol-controls. Filed with the rack's densities as the rung's numbers |
| 2026-09-01 | **`ControlsXsRung` returned and adopted within the hour** — the DS shipped the `xs` rung across theme + component and kol-controls 0.3.0 retired the three. Seven sites now read the DS at `xs`; kol-controls is exactly the set with no app twin. Verified live (rack + every swapped module's page, 0 errors), A/B re-shot. Four tickets round-tripped today: KolControlsPackage · ControlsJackSeams · ControlsXsRung, all 🟢 |
| 2026-09-01 | **`DropdownXsList` returned and adopted** — component 0.160.0 · theme 0.127.0; the xs list measured on the rung, nothing truncated. Every ticket filed today is 🟢: KolControlsPackage · ControlsJackSeams · ControlsXsRung · DropdownXsList |
| 2026-09-02 | **Selector restored, `StepperInlineVariant` filed** — the ControlsXsRung Selector swap changed the control's shape (the DS stacks its chevrons; the rack's is `‹ value ›` inline). Recorder ×3 + Raster back on the local Selector; the DS asked for `layout="inline"`. User: not reviewing until it is back from the DS |
| 2026-09-02 | **`StepperInlineVariant` returned and adopted; kol-shell 0.33.0 → 0.38.0 with the rail fork re-synced.** The inline Stepper was measured against the rack's Selector shape before any module took it. The shell bump brings the user's ruling from kol-mirror without re-explaining: Home lands on the collapsed 48px rail with a tap disc (`touch="shell"` on `/`, ShellRailCollapsedWithTapOpen), every other route keeps the drawer; `RackRail` ports NavRail 0.38.0's grab-as-button + `snapRef`. Measured at 390 under a coarse pointer: Home rail 48px with a 24×24 disc, tap → 264, `/rack` drawer closed + hamburger, 0 errors |
| 2026-09-02 | **Two rulings on the phone: the tap disc goes, and Home stays on the drawer.** The disc (`.kol-rail-grab-tap`) that arrived with kol-shell 0.38.0 was never approved — out of the fork, `RailGrabTapIsALine` filed to make the strip itself the opener. And Home reverts to `touch="drawer"`: the collapsed rail's 48px pushed the catalog at 390 under its two-column floor, one card the width of the screen; hidden, the grid snaps back to two |
| 2026-09-02 | **`RailGrabTapIsALine` returned and adopted** — shell 0.39.0 · theme 0.128.0; NavRail's grab is the bare button the fork already had. Six tickets round-tripped in this sitting, all 🟢 |
| 2026-09-02 | **`ContentCardActionsInsetShorthand` returned; bumped to component 0.162.0.** Create's INSERT moved to its own strip under the card meanwhile (user), so the fixed slot is unused here |
| 2026-09-02 | **`ShellDrawerCloseOnSamePath` returned and adopted** — kol-shell 0.40.0; the local Escape shim is gone |
| 2026-09-02 | **`IconButtonNoFlexShrink` filed** — the stage's bottom row: DS icon-only buttons rendered 22×32 in WebKit beside 32px neighbours; the row is a shrink-to-fit flex cluster and WebKit sizes it from the glyph, not the rule. Ask: `flex: none` on `.kol-btn-icon`. Row fixed here by giving it a definite width |
| 2026-09-02 | **`IconButtonNoFlexShrink` returned and adopted** — kol-theme 0.129.0, dev restarted `--force`; the stage row re-measured 32×32 at 390 and 1440. The row keeps its definite width (`left-4 right-4 justify-end`) |
